# Contributing

## Commits

One line, no trailer, no sign-off:

```
type(scope): message
```

`type` is one of feat, fix, refactor, chore, docs, style, perf, test, ci, build.
`scope` is the affected area within this repository.

## Pull requests

- Keep the diff minimal: change the lines the task requires and no others.
- CI must be green: lint, type-check, tests, and image scan where applicable.
- A change that affects performance carries a measurement, not an assertion.

## Reporting a security issue

Do not open a public issue. Mail canakyuz23@gmail.com.
